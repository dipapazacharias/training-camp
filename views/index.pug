extends layout

block content
  .jumbotron
    .container
      h1 #{title}
      block content
      p
        a.btn.btn-primary.btn-lg(href='#', role='button') Learn more &raquo;
      p this is a pug layout exmaple biatches!